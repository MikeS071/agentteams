# AS-13: Remove Predictor agent

## Scope
Remove the "Predictor" agent (id: `predictor`) from the entire codebase.

## Changes required

1. **`apps/web/src/lib/agents.ts`** — Remove the object with `id: "predictor"` from the `AGENTS` array
2. **`AGENT_ORDER`** in `apps/web/src/app/dashboard/chat/page.tsx` — Remove `"predictor"` from the array
3. **Agent images** — Remove `/public/images/predictor.png` if it exists
4. **Landing/pricing pages** — Remove any mention of Predictor from feature lists, marketing copy
5. **Static landing** — Check `static-landing/` for predictor references
6. **Update agent count** — After this + as-02 (removes leadgen + browser), total should be 6 agents: chat, research, coder, intel, social, clip

## Rules
- Branch: `feat/as-13`
- Commit: `refactor: remove Predictor agent`
- Verify: `cd apps/web && npx tsc --noEmit`
