# AS-19: Update user and technical documentation

## Scope
Update all documentation to reflect the UI refactor.

## User Documentation
Create/update `docs/user-guide.md`:
- Agent selection: how to choose an agent, what each does
- Agent modes: explain each agent's split UI layout
- Chat: how to start new chats, switch agents
- Swarm monitoring: collapsible panel + swarm tab
- Sidebar: icon navigation explanation
- Screenshots placeholders: note where screenshots should go

## Technical Documentation
Create/update `docs/technical/ui-architecture.md`:
- Component hierarchy diagram (text-based):
  - DashboardLayout → ChatPage → AgentModeSelector + AgentModeLayout
  - AgentModeLayout → ChatPanel + SidePanel (varies per agent)
  - SwarmStatus (compact in chat, full in swarm tab)
- Agent registry: how agents are defined in `lib/agents.ts`
- Adding a new agent: step-by-step guide
  1. Add to AGENTS array in `lib/agents.ts`
  2. Create side panel component (if split view)
  3. Wire into AgentModeLayout switch
  4. Add E2E test cases
- Data flow: how chat messages trigger side panel updates
- API endpoints: list all `/api/` routes with method + purpose

## README.md updates
- Update feature list (remove Hands, Lead Generator, Browser, Predictor references)
- Update agent list to show current 6
- Update any architecture diagrams

## Rules
- Branch: `feat/as-19`
- Commit: `docs: update user guide + technical docs for UI refactor`
- No build verification needed (docs only)
