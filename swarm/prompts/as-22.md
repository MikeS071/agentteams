# AS-22: Move model dropdown to chat header, remove from top navbar

## Scope
The model selection dropdown should be in the chat header bar (right side), NOT in the top navbar.

## File: `apps/web/src/app/dashboard/chat/page.tsx`

### Changes:
1. Move the model `<select>` / dropdown into the chat header bar, aligned right
2. Remove any model selector from the top navigation bar or dashboard layout
3. The dropdown shows models from `/api/models` with proper display names (as-24 will fix the names)
4. Selected model persists per agent (existing `modelSelections` state)

### Chat header final layout:
```
[Agent Name (active)] [Configure] [+ New] ────── [Model: ▾ anthropic/claude-opus-4-6]
```

## Rules
- Branch: `feat/as-22`
- Commit: `feat: model dropdown in chat header, removed from navbar`
- Verify build
