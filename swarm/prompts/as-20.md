# AS-20: Widen sidebar from w-16 to w-20 with visible text labels

## File: `apps/web/src/app/dashboard/layout.tsx`

Change sidebar width from `w-16` (64px) to `w-20` (80px). Ensure text labels below icons are visible and not truncated. If `w-20` is still too narrow for labels, use `w-24` (96px).

Each nav item should show: icon (h-5 w-5) + label text (text-[11px]) clearly readable.

## Rules
- Branch: `feat/as-20`
- Commit: `fix: widen sidebar for visible text labels`
- Verify: `cd apps/web && npx tsc --noEmit`
