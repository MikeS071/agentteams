# AS-07: Coder — full Chat UI mode

## Scope
When "Coder" agent is active, render the full-width Chat UI with no side panel.

## Implementation
- Use AgentModeLayout with no `sidePanel` prop → renders chat at full width
- The chat UI is the standard chat input + messages area
- No additional panels needed — code output renders in chat via CodeBlock component

## Rules
- This should be the simplest agent mode — just pass `splitRatio="1fr"` or no sidePanel
- Branch: `feat/as-07`
- Commit: `feat: Coder agent full chat UI mode`
- Verify build passes
