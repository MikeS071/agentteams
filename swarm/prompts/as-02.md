# AS-02: Remove Lead Generator and Browser Agent

## Scope
Remove the "Lead Generator" (id: `leadgen`) and "Browser Agent" (id: `browser`) from the agent list and all related UI.

## Changes required

1. **`apps/web/src/lib/agents.ts`** — Remove the objects with `id: "leadgen"` and `id: "browser"` from the `AGENTS` array
2. **`AGENT_ORDER`** in `apps/web/src/app/dashboard/chat/page.tsx` — Remove `"leadgen"` and `"browser"` from the array
3. **Any agent images** — Remove `/public/images/leadgen.png` and `/public/images/browser.png` if they exist
4. **Landing/pricing pages** — Check `apps/web/src/app/pricing/page.tsx` and any landing pages. Remove references to Lead Generator and Browser Agent from feature lists, pricing tiers, etc.
5. **Homepage/marketing copy** — Check `static-landing/` and any page referencing these agents. Remove them.
6. **API routes** — If there are specific API routes for leadgen or browser agent features, remove them
7. **Agent setup/config** — Check `apps/web/src/components/AgentSetup.tsx` for any leadgen/browser-specific config UI

## Rules
- After removal, there should be 7 agents: chat, research, coder, intel, social, clip, predictor
- Update the agent count display in AgentGrid.tsx header (currently shows "8 agents")
- Branch: `feat/as-02`
- Commit: `refactor: remove Lead Generator and Browser Agent`
- Verify build: `cd apps/web && npx tsc --noEmit`
